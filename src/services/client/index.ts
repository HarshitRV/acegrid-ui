import * as auth from './auth'
import * as courses from './courses'
import * as exams from './exam'
import * as attempts from './attempt'

const client = { auth, courses, exams, attempts }

export default client
