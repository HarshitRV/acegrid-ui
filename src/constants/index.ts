export const SESSION_STORAGE_AUTH_TOKEN_KEY = 'auth_token'

const examForm = {
  id: 'exam-form',
} as const

const courseForm = {
  id: 'course-form',
} as const

export const formIdMap = {
  examForm,
  courseForm,
} as const
